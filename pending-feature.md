Pending / Excluded Features (Per Your Request)
Subscription System: We explicitly skipped the yearly subscription logic (payment integration, expiry dates, etc.).
Premium Gating (Locking Surahs): Since the subscription feature was excluded, all Surahs are currently "unlocked." The logic to lock everything except Surah Al-Fatiha is pending until you decide to add the subscription module.
Frontend (Flutter) UI: The following UI-specific elements need to be built by the Flutter developer using the APIs I've provided:
The lightbulb icon for Tafsir.
The Circular Live Counter (UI element only) that updates while scrolling.
The "X" button to hide the counter.
The "Collect Hasanat" button at the end of Surahs.
❗ Summary
The Core Backend Logic for all new features is 100% complete. The only things remaining are the Subscription/Gating logic (which you asked to skip) and the Frontend UI implementation (which the Flutter developer will do using the new documentation I created).


also donation button
it's one time payment or subscription basis